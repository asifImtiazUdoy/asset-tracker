# Database configuration (optional for running without database)
DATABASE_URL=postgresql://user:password@localhost:5432/database_name

# Node environment
NODE_ENV=production

# Port configuration (Vercel sets this automatically)
PORT=3000
